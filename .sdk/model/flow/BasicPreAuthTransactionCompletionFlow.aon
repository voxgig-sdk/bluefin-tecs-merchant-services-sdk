# BasicPreAuthTransactionCompletionFlow

main: kit: flow: BasicPreAuthTransactionCompletionFlow:
{
  entity: "pre_auth_transaction_completion",
  kind: "basic",
  name: "BasicPreAuthTransactionCompletionFlow",
  step: [
    {
      input: {
        ref: "pre_auth_transaction_completion_ref01"
      }

      op: "create"
    }
  ],
  "key$": "BasicPreAuthTransactionCompletionFlow"
}